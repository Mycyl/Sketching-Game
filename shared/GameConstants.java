package shared;

/**
 * Defines shared constants for the game (e.g., ports, buffer sizes, timers).
 * Used by both client and server for consistency.
 */

// static class
// Holds constants like PORT, buffer sizes, timer values.


public class GameConstants {

    public static final int SERVER_PORT = 12345; // Server port number
    public static final int TIMER_DURATION = 90; // Duration of each round in seconds
    
}
